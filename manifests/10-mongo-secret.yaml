# NOTE: Replace with real secret values before production.
# Generate with: kubectl create secret generic mongo-secret --from-literal=...
# Values below are base64-encoded (admin / admin123)
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
  namespace: todo-app
  labels:
    app.kubernetes.io/name: mongo-secret
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: todo-app
type: Opaque
data:
  # echo -n 'admin' | base64
  MONGO_INITDB_ROOT_USERNAME: YWRtaW4=
  # echo -n 'admin123' | base64
  MONGO_INITDB_ROOT_PASSWORD: YWRtaW4xMjM=
